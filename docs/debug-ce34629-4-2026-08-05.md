# Debug Log: CE-34629-4 — 2026-08-05

> 2026-08-05 session. Goal: get a launchable `PPLA00001` homebrew package
> on FW 11.00 + GoldHEN. Latest build still fails with CE-34629-4 despite
> the flat-`eboot.bin` fix (Bug #19).
>
> This log records every probe that was attempted and its result, so the
> next variant starts from facts, not memory.

---

## Environment recap

| Item | Value |
| --- | --- |
| PS4 | FW 11.00 + GoldHEN |
| PS4 IP / FTP | `192.168.2.105:2121` (GoldHEN FTP v2.2, absolute paths required) |
| Backend | `192.168.2.166:8765` |
| PkgTool host | hits ICU error — must run inside `ps4-uk-build` docker image |
| Project | `/home/rorschach/UA flims/ps4-uk-stream` (note the space!) |

## Artifacts on the table

| File | Size | SHA-256 | Source |
| --- | --- | --- | --- |
| `pplay-fork/build/PPLA00001.pkg` = `build/IV0000-...pkg` | 14,352,384 B | `51255e23…` | current build, uploaded to PS4, **fails CE-34629-4** |
| `build/bug-18-verification.txt` record | 15,269,888 B | `6a2a0b7f…` | previously verified build (18:56) |
| `docs/ps4-test-report.md` record | — | `66617518…` | third, matches neither |
| `IV0000-PPLA00001_00-PPLAY00000000000.pkg` | 7,012,352 B | — | older build, still on PS4 |
| `IV0001-PPLA00001_00-PPLA000013080000.pkg` | 33,882,112 B | `06769829…` | upstream pplay 3.8, on PS4 + pulled to `/tmp/upstream-pplay-3.8.pkg` |

Key anomaly: **the artifact we actually tested (14,352,384 B) differs from
the build that was verified (15,269,888 B).** Those are different packages.

## Attempts and results

### A. Extract + compare payload trees (docker ps4-uk-build)

Extract both PKGs via `pkg_extract` → `uroot/`:

- `docker run --rm -v ... --entrypoint sh ps4-uk-build -c
  '/opt/oo/bin/linux/PkgTool.Core pkg_extract <pkg> <out>'`
- Result: **our package extracts cleanly. No parse/decrypt errors.**
- File trees are identical to upstream except we additionally ship
  `mpv/subfont.ttf`.
- All shared payload files have **identical sha256** to upstream:
  `keystone`, `right.sprx`, `libc.prx`, `libSceFios2.prx`, all `skin/*`,
  the font.
- Our `eboot.bin` is 4,353,904 B (stub build, player is issue #65);
  upstream's is 30,258,816 B. Payload content is not the differentiator.

### B. `pkg_validate` (docker)

Our package vs upstream, `pkg_validate --verbose`:

- Both: **all hashes and signatures OK, RC=0.**
  (entry digests, PKG header, body digest, PFS digests, PFS signed digest,
  IMAGE_KEY/layout, LICENSE_INFO, PARAM_SFO digest, PLAYGO digests,
  ICON0 digest, SC entries, Debug RIF signature…)

### C. `pkg_listentries` (docker)

Entry tables are **structurally identical** (same 14 entries, same flags,
same ordering). The only differences:

| Entry | Ours | Upstream |
| --- | --- | --- |
| PARAM_SFO | 0x3D8 (984 B) @ 0x3AD0 | 0x450 (1104 B) @ 0x3F80 |
| PLAYGO_CHUNK_SHA | 0x36C (876 B) | 0x814 (2068 B) |
| (everything else) | identical | identical |

PLAYGO is non-critical for a fake pkg; the PARAM_SFO size gap is expected
(more/fewer fields), not a format difference.

### D. SFO extraction via `pkg_extractentry`

Command: `PkgTool.Core pkg_extractentry <pkg> 9 <out>` (entry 9 = PARAM_SFO).

- Extracts fine to `/tmp/sfo_ours.bin` (984 B) and `/tmp/sfo_up.bin`
  (1104 B). Sizes match the entry table.
- **Analysis aborted by user** before field-by-field comparison — trying a
  different approach instead.

---

## Standing conclusions (not revisited by the next variant)

1. The package is **structurally valid** per PkgTool (extracts, validates,
   entry table sane).
2. **The tested artifact is not the verified one** (14,352,384 B vs
   15,269,888 B). Before blaming the toolchain, rule out that we simply
   re-uploaded an older/different build.
3. Payload (eboot aside) is byte-identical to working upstream 3.8.
4. Correctly formatted SFO fields in worktree (CATEGORY `gde`,
   SYSTEM_VER 0x3FC=1020, ATTRIBUTE 0x10000, CONTENT_ID
   `IV0000-PPLA00001_00-PPLAY00000000000`) are already confirmed from the
   worktree sources, but **the SFO embedded by the builder was never
   verified field-for-field** — the D analysis was the first attempt.

## Reference files

- `/tmp/upstream-pplay-3.8.pkg` — upstream 3.8 pulled from PS4
- `/tmp/ourpkg/uroot/`, `/tmp/upstrpkg/uroot/` — extracted trees
- `/tmp/sfo_ours.bin`, `/tmp/sfo_up.bin` — extracted PARAM_SFO
- `pplay-fork/build/pkg.gp4` — gp4 of current build (flat `eboot.bin`)