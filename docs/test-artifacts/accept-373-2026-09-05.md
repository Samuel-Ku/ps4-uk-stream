# #373 acceptance — one English title with seek + subtitles (real engine → Switchfin)

**Ticket:** #373 (prototype, spec #365) — *Resolved with a short findings
note + the working recipe.* **Status:** IN PROGRESS — this file is updated
in place as each stage lands.

## Environment (verified 2026-09-05)

- This workstation **is `192.168.2.166`** — the operator's documented
  engine host (`docs/torrent-lane.md` §1 env sample) — on the same /24
  as the PS4 and the Switchfin device (adb address from
  `docs/test-artifacts/switchfin/device-driving.md`).
- Docker available but socket is root-gated on this box; the compose
  stack is `backend/deploy/docker-compose.bitplay.yml` (runbook §1).
- No production backend runs here (`cs-uk-api.service` absent); ports
  3347 (engine) and 8003 (the Switchfin device port) are free.
- Internet egress verified (yts.gg answers 200 from this box).
- Test title: **Sintel** (Blender Foundation, freely distributable,
  well-seeded, carries a separate `.srt`) — the runbook §5 magnet.

## The working recipe (executable form)

The runbook §5 protocol, made re-runnable:

- `backend/deploy/accept_373.sh {engine|facade|all}` — engine hand-drive
  (runbook steps 1–4) plus the player-floor checks F1–F4, then the same
  title through the real backend (B1–B5).
- `backend/deploy/run_backend_8003.sh` — the real backend on :8003 (the
  Switchfin device port) with `CS_UK_TORRENT_ENGINE_URL` pre-wired.

## Results

| Stage | Check | Result |
|---|---|---|
| engine | capabilities (ffmpeg present) | PENDING — docker approval |
| engine | F1 progressive start | PENDING |
| engine | F2 Range seek (206 + Accept-Ranges) | PENDING |
| engine | F3 srt→VTT | PENDING |
| engine | F4 audio track (ffprobe) | PENDING |
| facade | B1 search → group key | PENDING |
| facade | B2 PlaybackInfo engine truth | PENDING |
| facade | B3 stream 302 → engine URL | PENDING |
| facade | B4 subtitle DeliveryUrl | PENDING |
| facade | B5 bytes through redirect chain | PENDING |
| device | Switchfin (adb) or PS4 on-device pass | PENDING |

## Spec facts learned

(to be filled as stages land; anything that breaks here becomes a spec
constraint per the ticket)

## Operator handoff

(what remains for the PS4-owner pass once this file's checks are green)
