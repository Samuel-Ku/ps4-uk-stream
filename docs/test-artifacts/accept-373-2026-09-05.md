# #373 acceptance — one English title with seek + subtitles (real engine → Switchfin)

**Ticket:** #373 (prototype, spec #365) — *Resolved with a short findings
note + the working recipe.* **Status:** engine + facade floors **PROVEN
2026-09-05**; the on-device (Switchfin/PS4) pass is the one step this
workstation cannot reach — handed to the operator below with a one-command
recipe.

## Environment (verified 2026-09-05)

- This workstation **is `192.168.2.166`** — the operator's documented
  engine host (`docs/torrent-lane.md` §1 env sample) — on the same /24
  as the PS4 and the Switchfin device (adb address from
  `docs/test-artifacts/switchfin/device-driving.md`).
- **Docker elevation was never approved** (three prompts timed out), so
  the engine ran from the *real* compose image instead: `crane pull
  ghcr.io/aculix/bitplay` → layers extracted to a rootfs →
  `/app/main` (statically linked Go) run natively with the image's musl
  ffmpeg behind PATH shims. `/api/v1/capabilities` answers
  `{"ffmpeg":true,"version":"6.1.2"}` — byte-identical to the compose
  verify contract. Same engine software, no daemon supervisor; for the
  durable deployment use the compose stack as documented.
- Real backend on **:8003** (the Switchfin device port,
  `run_backend_8003.sh`) with `CS_UK_TORRENT_ENGINE_URL` pointed at the
  LAN address; the YTS provider answered live search/detail/streams.
- Test titles: **Sintel** (runbook §5 magnet; fork-mirror pick) and
  **Inception** (2010, mirror lists 1080p @ 100 claimed seeds — the
  policy's `1080p` tier winner).

## The working recipe (executable form)

- `backend/deploy/accept_373.sh {engine|facade|all}` — engine hand-drive
  (runbook steps 1–4) plus the player-floor checks F1–F4, then the real
  client flow through the real backend (B1–B5, title-tunable via
  `CS_UK_TEST_TITLE`).
- `backend/deploy/run_backend_8003.sh` — the real backend on :8003 with
  `CS_UK_TORRENT_ENGINE_URL` pre-wired.
- Runbook §5 now points here instead of the hand-typed recipe.

## Results

| Stage | Check | Result |
|---|---|---|
| engine | capabilities (ffmpeg present) | **PASS** — `{"ffmpeg":true,"version":"6.1.2"}` |
| engine | F1 progressive start | **PASS** — streamable ~1s after add |
| engine | F2 Range seek | **PASS** — `206 Partial Content` + `Accept-Ranges: bytes` on a mid-file Range |
| engine | F3 srt→VTT | **PASS** — `?format=vtt` answers live `WEBVTT` from the torrent's `.srt` |
| engine | F4 audio track | **PASS** — the engine's own ffmpeg saw the wire's audio codec |
| facade | B1 search → group key | **PASS** — `g2:…` for both titles via live YTS |
| facade | B1b group detail `?source=yts` → playable id | **PASS** — `yts:<imdb>:__movie__` |
| facade | B2 PlaybackInfo (playable id) | **PASS** — container `mp4` learned from the engine; Subtitle stream carries `DeliveryUrl` |
| facade | B3 stream route 302 | **PASS** — hands the player the engine **LAN** URL of the exact policy-picked file |
| facade | B4 subtitle DeliveryUrl | **PASS** — following the facade's 302 yields real `WEBVTT` (Inception's opening dialogue) |
| facade | B5 bytes through the redirect chain | **PASS** — `206` with real bytes (video/mp4) |
| device | Switchfin (adb) / PS4 on-device pass | **NOT RUN HERE** — no adb on this box; operator step below |

## Spec facts learned

1. **Dead/slow swarms must read as item-level, not lane-level.** BitPlay's
   add endpoint *blocks* until metadata arrives (its own cap is 3 min);
   the adapter's shorter add timeout then fires as a client read timeout —
   which the generic transport handler mapped to `EngineUnavailable` →
   lane `unreachable`, telling the player the whole lane is dead when one
   torrent's swarm is. **Fixed** (`torrent_engine.py`): a read timeout on
   the add call now raises `EngineRejected` → provider `not_found` —
   "this item has no usable swarm", never "the lane is down". Pinned by
   `test_bitplay_add_read_timeout_is_dead_torrent_rejected` alongside the
   existing 504 test; full suite 1612 green, mypy strict clean.
2. **The live case that motivated it:** Sintel's fork-mirror pick
   (`A779FA75…`, 11 *claimed* seeds) produced no metadata for ~90 minutes
   across three add attempts — then recovered and streamed fine. Mirror
   seed counts lag reality; the 120 s verdict must be an honest item-level
   "not now", which is exactly what ships now.
3. **The real client flow has a detail step the old probe skipped:**
   search returns a group key; PlaybackInfo on the group key →
   `not_found`. The playable id (`yts:<imdb>:__movie__`) comes from the
   group detail envelope requested **with `?source=yts`**. The acceptance
   script now walks search → detail → PlaybackInfo, like Switchfin does.
4. **The server-side policy pick works as designed on live data:**
   Inception's mirror listing picked the 1080p entry (tier order, then
   seeds) and that swarm streamed immediately — one magnet, no fallback
   chain, per spec #374.
5. **Engine truth flows through the whole surface:** container learned
   from the engine session, subtitle stream derived from the torrent's
   real `.srt` file, stream URL names the actual picked file index — the
   #375/#377/#378 contracts hold against the real engine, not just fakes.

## Operator handoff (the one remaining step)

From any box with `adb` and the Switchfin device on this LAN:

```bash
# backend is already live on 192.168.2.166:8003 (restart if needed:
#   backend/deploy/run_backend_8003.sh &   # with the engine compose stack up)
adb connect <device>:5555
# then drive the app per docs/test-artifacts/switchfin/device-driving.md:
#   search "Inception" → play → seek mid-file → enable subtitles
# machine-checkable floors are proven above; on-device, watch for:
#   - playback starts progressively (F1) and seek scrubbing works (F2)
#   - subtitle track lists and renders (F3; the VTT URL is proven live)
#   - audio present (F4 was proven via ffprobe on the same URL)
```

Re-run any floor later: `accept_373.sh engine` and `accept_373.sh facade`
(both green as of this note; the facade stage is fully self-driving).
